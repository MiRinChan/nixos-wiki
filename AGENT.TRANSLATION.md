# 智能体翻译指南

本文面向使用智能体翻译 <https://wiki.nixos.org> 的工作流。通用规则和词汇表以
[TRANSLATION.md](TRANSLATION.md) 为准。

## 翻译前

- 先阅读 [TRANSLATION.md](TRANSLATION.md)、`AGENT.md` 和 `CLAUDE.md`。
- 确认源页面 URL、目标词条路径和翻译范围。
- 检查已有相近词条、分类和模板，沿用本仓库的写法。
- 如果任务涉及 NixOS、nixpkgs、Home Manager、flake、频道、选项或包等事实性内容，查询时优先使用 `mcp-nixos`，不要凭记忆猜。

## 翻译流程

1. 读取源页面，记录页面标题、章节结构、内部链接、模板、图片和分类。
2. 把 MediaWiki 语法转换为本仓库支持的 Markdown、HTML 或模板写法。
3. 按章节翻译，保持源文结构和技术含义。
4. 对照 [TRANSLATION.md](TRANSLATION.md) 的词汇表处理术语。
5. 对 `待定` 术语不要自行定译；第一次出现时保留英文或写作 `暂用译法（English term）`，并在同一篇文章内保持一致。
6. 保留命令、代码、配置、路径、URL、选项名、包名和属性路径。
7. 源文疑似过期时，不要静默改写。先核实事实，再把必要的中文补充和源文翻译区分开。

## 自检要求

提交给人类前，智能体必须逐项检查：

- `已定` 术语是否全部遵守词汇表。
- 是否有 `待定` 术语被擅自固定译法。
- 代码块、行内代码、命令、配置和路径是否保持可复制。
- 源站链接是否转换为有效的仓库链接或保留为完整 URL。
- MediaWiki 模板是否已经转换，不存在本仓库无法展开的模板调用。
- 图片和附件是否放在对应词条目录内。
- 标题、脚注、alert、分类语法是否能被本仓库构建器处理。
- 译文是否忠实于源文，没有编造段落或省略关键警告。

## 禁止事项

- 不要为了让译文更顺口而改变命令、配置或技术结论。
- 不要把未经核实的新事实混入源文翻译。
- 不要用大模型判断 NixOS 选项、包名或频道现状是否正确；需要核实时使用 `mcp-nixos`。
- 不要把 `待定` 术语写进词汇表的固定译法列。
- 不要提交无法被本仓库构建器处理的 MediaWiki 残留语法。

