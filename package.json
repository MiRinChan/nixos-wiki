{
  "name": "nixos-wiki",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && concurrently \"chokidar 'entries/**/*.md' 'template/**/*.md' 'index.md' 'template.html' 'styles-base.css' 'styles-code-light.css' 'styles-code-dark.css' -c 'npm run build'\" \"live-server out --open\""
  },
  "dependencies": {
    "highlight.js": "^11.11.1",
    "marked": "^16.4.1"
  },
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "concurrently": "^9.2.1",
    "live-server": "^1.2.2"
  }
}
