{
  "name": "nixos-wiki-that-my-personal-change-which-is-in-chinese",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "concurrently \"chokidar 'entries/**/*.md' 'index.md' 'template.html' -c 'npm run build'\" \"live-server out --open\""
  },
  "dependencies": {
    "marked": "^16.4.1"
  },
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "concurrently": "^9.2.1"
  }
}
